(function(){const KEY='tomatoDiaryData_v2';const data=JSON.parse(localStorage.getItem(KEY));if(!data){console.log('저장된 데이터가 없습니다.');return;}let removed=0;Object.keys(data.days).forEach(function(key){const day=data.days[key];const before=day.todos.length;day.todos=day.todos.filter(function(t){const isTarget=t.title==='문풀' && !t.categoryId;if(isTarget) removed++;return !isTarget;});});localStorage.setItem(KEY,JSON.stringify(data));console.log(removed+'개의 미분류 "문풀" 항목을 삭제했습니다. 페이지를 새로고침하세요.');})();

(function(){const KEY='tomatoDiaryData_v2';const data=JSON.parse(localStorage.getItem(KEY));if(!data){console.log('저장된 데이터가 없습니다.');return;}const targetMonths=['2026-09','2026-10','2026-11'];let removed=0;Object.keys(data.days).forEach(function(key){if(targetMonths.some(function(m){return key.indexOf(m)===0;})){delete data.days[key];removed++;}});localStorage.setItem(KEY,JSON.stringify(data));console.log(removed+'일치 데이터를 삭제했습니다. 페이지를 새로고침하세요.');})();
